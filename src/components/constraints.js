
/*
Returns true if the move is possible, false otherwise
*/
export function isValid(from, to, board, cb) {

    console.log('from', from);
    console.log('to', to);
    console.log('board', board);


    cb('Invalid move');

}